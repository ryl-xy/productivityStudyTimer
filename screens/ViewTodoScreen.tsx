import React from 'react';
import {View} from 'react-native';
import {ViewTodoScreenProp} from '../types/navigation';

export default function Main({navigation, route}: ViewTodoScreenProp) {
  return <View></View>;
}
